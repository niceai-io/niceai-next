export const APP_NAME = "Nice AI";

const config = {
  title: APP_NAME,
  description:
    "Nice AI is a large-scale model application orchestration system that provides out of the box data processing, model calling, and other capabilities. It can quickly build a knowledge base and perform workflow orchestration through Flow visualization, achieving complex knowledge base scenarios!",
};

export default config;
